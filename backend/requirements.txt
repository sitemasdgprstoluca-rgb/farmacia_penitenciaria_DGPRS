# =============================================================================
# DEPENDENCIAS DE PRODUCCIÓN - Farmacia Penitenciaria
# =============================================================================
# Versiones fijadas para garantizar reproducibilidad
# Actualizar con: pip freeze > requirements.txt

# Core Django
Django==5.2.8
djangorestframework==3.15.2
djangorestframework-simplejwt==5.5.1
django-cors-headers==4.3.1
django-filter==23.5

# Utilidades
openpyxl==3.1.2
reportlab==4.0.9
Pillow>=10.4.0  # Compatible con Python 3.13

# Configuración de entorno
python-decouple==3.8

# Email (Resend para producción)
resend==2.0.0

# Archivos estáticos en producción
whitenoise==6.6.0

# Redis para caching (opcional pero recomendado)
redis==5.0.1
django-redis==5.4.0
hiredis==2.3.2

# Documentación API
drf-spectacular==0.27.1
pyyaml==6.0.1
uritemplate==4.1.1

# PostgreSQL para Render/Supabase (psycopg3 compatible con Python 3.13)
psycopg[binary]>=3.1.0
dj-database-url==2.1.0

# ISS-001/002: Supabase Storage para documentos de lotes
supabase>=2.0.0

# Servidor WSGI para producción
gunicorn==22.0.0

# Seguridad y validación
cryptography==44.0.1

# HTTP requests (para reCAPTCHA y APIs externas)
requests>=2.31.0

# Utilidades Python
pytz==2024.1
python-dateutil==2.8.2

